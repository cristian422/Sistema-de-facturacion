import panda as pd
from SISTEMAS_CLASES.Caja import caja

    class ArchivoCaja:
    caja: persona 
    def EnviarFactura(Factura) -> True:
    factura= str
    def generarTXT(init):
    pd.Dataframe(Factura).to_csv("Factura.cvs")


